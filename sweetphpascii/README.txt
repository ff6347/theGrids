parameters:
demo=true // you can see it in the browser to load leave the demo away
char=fabianthelbind // enters theese chars as characters URL Encoding + = " "
ellify=  true //takes the max value, without he takes 255 as maxvalue
file= // enter a url for a png to asciiify
an & separates the parameters

like this http://project.the-moron.net/sweetphpascii/sweet_asciiify.php?demo=true

this is the raw string for indesign.
http://project.the-moron.net/sweetphpascii/sweet_asciiify.php

